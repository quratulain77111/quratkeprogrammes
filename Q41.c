//Display a string from the user.
#include<stdio.h>
int main()
{
    char str[100];
    printf("Enter the string\n");
    scanf("%s",&str);
  
    //Print the string.
   
        printf("%s\t", str);
 
      return 0;
}